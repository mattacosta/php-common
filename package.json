{
    "name": "@mattacosta/php-common",
    "description": "Base classes and interfaces that define commonly used data types and methods.",
    "version": "1.0.0-beta.3",
    "author": "Matt Acosta",
    "license": "Apache-2.0",
    "repository": {
        "type": "git",
        "url": "https://github.com/mattacosta/php-common.git"
    },
    "bugs": {
        "url": "https://github.com/mattacosta/php-common/issues"
    },
    "files": [
        "lib/src/**/*.js",
        "typings/"
    ],
    "main": "./lib/src/main.js",
    "typings": "./typings/main.d.ts",
    "devDependencies": {
        "@types/mocha": "^5.2.7",
        "@types/node": "^10.14.9",
        "coveralls": "^3.0.4",
        "mocha": "^6.1.4",
        "nyc": "^14.1.1",
        "typescript": "^3.5.2"
    },
    "scripts": {
        "build": "tsc -b",
        "pretest": "tsc -b",
        "test": "mocha --recursive ./lib/test",
        "test:coveralls": "nyc report --reporter=text-lcov | coveralls",
        "test:nyc": "nyc --extension .ts mocha --recursive ./lib/test",
        "watch": "tsc -b -w"
    }
}
